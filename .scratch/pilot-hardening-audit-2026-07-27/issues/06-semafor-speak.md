# 06 — Семафор TTS-конкурентности на `/speak` (N12)

**What to build:** Прямой `/speak` больше не может занять единственный TTS/GPU-ресурс без
ограничения: тот же лимит одновременных обращений, что защищает `/voice`, применяется и к
`/speak`.

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] `/speak` ограничен тем же лимитом конкурентности TTS/GPU, что и `/voice`.
- [ ] Обращения сверх лимита сериализуются (ждут очереди, не отвергаются) — наблюдаемо
      без завязки на тайминги.
- [ ] Тест покрывает ограничение конкурентности на `/speak`; набор тестов зелёный.
