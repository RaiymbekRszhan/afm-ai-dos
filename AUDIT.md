# Аудит проекта Ai-dos (STT → RAG → TTS)

_Дата: 2026-07-24 · ветка `fix/rag-language-tagging` · бэкенд запущен, все сервисы отвечают._

Полный статический разбор кода + проверка живого стека. Секреты (`.env`, ключи) в
аудит не входят — доступ к ним закрыт правами, читалось только `config.py`/дефолты.

---

## 1. Итог (TL;DR)

Проект **в хорошем инженерном состоянии**: чистое разделение сервисов по venv и
HTTP-границам, серьёзная антигаллюцинационная обвязка RAG, аккуратнейшая
нормализация текста под TTS (падежи, числа ru/kk, телефоны группами), XSS-безопасный
офлайн-фронтенд, везде мягкая деградация вместо падений. Тесты зелёные
(**51 pytest + 16 node**), все 4 сервиса на проде отвечают.

**Блокеров, ломающих пилот, не найдено.** Но есть несколько **операционных
рассинхронов**, которые на демо/24-7 выстрелят: ложный «красный» F5 в health,
темп казахской озвучки настроен под ElevenLabs (а стек офлайн — Spark), и
единая точка отказа — GPU-нода АФМ. Ниже — по severity.

| # | Severity | Тема | Где |
|---|----------|------|-----|
| F1 | 🟠 High | Health F5 всегда `reachable:false` (нет `/health` на GPU-F5) | `app/clients/tts.py`, `scripts/healthcheck.sh` |
| F2 | 🟠 High | Темп казахской речи 1.0 настроен под ElevenLabs, а стек — Spark (медленный) | `video_ui/static/index.html:321` |
| F8 | 🟠 High | SPOF: LLM+эмбеддинги+STT+F5 — всё на `192.168.165.2`, фолбэка нет | архитектура |
| F6 | 🟡 Medium | `:8000` слушает `0.0.0.0` без authn, `/docs` открыт | `run_api.sh:94`, `app/main.py:88` |
| F3 | 🟡 Medium | `tts_enabled=true`, даже если `F5_URL/SPARK_URL` пусты | `app/config.py:143-157` |
| F12| 🟡 Medium | Весь текст ответа (+таблица) едет в HTTP-заголовке `X-Answer` | `app/main.py:252-268` |
| F4 | ⚪ Low | Дрейф print-регэкспов Python↔JS (`подач` vs `податч`) | `app/service.py:167`, `answer_render.js:145` |
| F5 | ⚪ Low | Пустой ответ LLM уводит в лишний `suggest`-путь (доп. LLM-вызов) | `rag/ragsvc/rag_engine.py:124` |
| F9 | ⚪ Low | Бэкапы `rag_storage.bak.*` копятся без ротации | `rag/` |
| F10| ⚪ Low | Видео (21 МБ) и `.docx` закоммичены в git | `video_ui/static/video/`, `rag/data/raw/` |
| F11| ⚪ Low | Мёртвая переменная `F5=:8810` в шапке healthcheck | `scripts/healthcheck.sh:18` |

---

## 2. Что проверено на живом стеке

```
orchestrator :8000  status=ok, rag.reachable=true, tts.enabled=true (ru=f5, kk=spark)
rag          :8077  status=ok, mode=naive, rerank=false  → работает языковой дедуп (LANG_DEDUP)
video_ui     :8100  status=ok, backend_reachable=true, videos: idle.mp4 ✓ talk.mp4 ✓
spark        :8809  status=ok, gender=male, cloning=false
F5 (GPU АФМ) :8991  /health → HTTP 404 (см. F1) — хост жив, роут отсутствует
```

- `pytest` (оркестратор): **51 passed**.
- `node --test answer_render.test.js`: **16 passed**.
- База RAG: 26 файлов, 13 пар ru/kk, у каждого корректный заголовок
  `# LAW/SHORT/TYPE/LANG`. Покрыты УК, УПК, КоАП, АПК, Закон о ПОД/ФТ, коллекторы,
  ЧАВО, памятка о мошенничестве, финпирамиды, Положение об АФМ, внутренний контроль,
  учёт СФМ.
- Гигиена репозитория: `.env` **не** в git, бэкапы/venv/модели в `.gitignore`,
  deny-list прав на секреты корректен.

---

## 3. Находки

### 🟠 F1 — Health-чек F5 всегда ложно-отрицательный

`app/clients/tts.py` строит адрес проверки из `F5_URL`, подменяя путь на `/health`:

```
F5_URL = http://192.168.165.2:8991/tts  →  _health_from → .../8991/health  →  HTTP 404
```

GPU-сервер F5 АФМ (multipart-контракт) роута `/health` **не имеет** — проба сейчас
возвращает 404. Последствия:

- `/health` оркестратора **всегда** показывает `tts.servers.f5.reachable=false`,
  даже когда синтез реально работает. Оператор либо паникует на ровном месте, либо
  привыкает игнорировать красный F5 — и пропустит **настоящий** отказ.
- `scripts/healthcheck.sh` читает `tts.servers.f5.reachable` → приёмка деплоя
  **всегда FAIL по F5** при удалённом GPU. «ГОТОВО: деплой принят» недостижимо.
- Watchdog не затронут (смотрит только общий `status: ok`).

**Рекомендация:** для multipart-режима (когда задан `F5_REF_AUDIO`) проверять
живость не по `/health`, а лёгким реальным `POST /tts`, ЛИБО ввести отдельный
`F5_HEALTH_URL`, ЛИБО помечать F5 как `degraded`/`unknown` вместо `false`, когда
`/health` даёт 404 (host отвечает ⇒ сервер жив).

---

### 🟠 F2 — Темп казахской озвучки рассинхронизирован со стеком

`video_ui/static/index.html:321`:

```js
const SPEECH_RATE = { kazakh: 1.0, russian: 1.0 };
```

Комментарий рядом прямо говорит: `kazakh:1.0` верен **только если казахский идёт
через ElevenLabs**; при откате на Spark вернуть `kazakh: 1.4` (Spark говорит
медленно, ~8 c на фразе). Но:

- сеть АФМ **без интернета** (ElevenLabs недоступен),
- живой стек использует **Spark** (`/health`: `kk=spark`).

⇒ На пилоте **казахский ответ будет проигрываться заметно медленно**. Нужно
определиться: (а) пилот на ElevenLabs — тогда нужен согласованный доступ к
`api.elevenlabs.io`, или (б) офлайн на Spark — тогда `kazakh: 1.4`. Сейчас
конфигурация внутренне противоречива.

---

### 🟠 F8 — Единая точка отказа: нода `192.168.165.2`

На одном адресе АФМ висят LLM (`:8901`), эмбеддинги (`:8806`), STT (`:8804`) и
F5-TTS (`:8991`). При недоступности ноды падает **весь** пайплайн: RAG не получит
ни эмбеддинги, ни LLM ⇒ и русский, и казахский ответы невозможны (даже локальный
Spark озвучивать нечего). Локальный watchdog перезапускает только `ai-dos-api` —
против отказа внешней ноды он бесполезен. Для режима 24/7 это ключевой риск.

**Рекомендация:** зафиксировать SPOF в `PILOT.md`, согласовать с IT АФМ доступность/
резерв ноды; продумать «дежурную» офлайн-фразу, если RAG недоступен целиком (сейчас
клиент получит 502 «Ошибка поиска по базе»).

---

### 🟡 F6 — Оркестратор открыт в сеть без аутентификации

`run_api.sh:94` поднимает `uvicorn app.main:app --host 0.0.0.0 --port 8000`, а
`app/main.py:88` отдаёт Swagger `/docs`. Любое устройство в сети киоска может
напрямую гонять дорогой пайплайн и дёргать внутренний vLLM АФМ (через
`/chat`, `/speak`, `/transcribe`, `/voice`).

Частично митигировано: семафор `max_concurrent_voice=2`, лимит аплоада 25 МБ,
лимит длины вопроса. Но нет ни authn, ни rate-limit. RAG-сервис сделан правильно
(слушает `127.0.0.1`).

**Рекомендация:** т.к. `video_ui` уже проксирует запросы, оркестратор можно
свести на `--host 127.0.0.1` (или адрес киоска) + firewall; `/docs` в проде
закрыть флагом.

---

### 🟡 F3 — `tts_enabled` не проверяет фактическую конфигурацию

`app/config.py:143-157`: для провайдеров `f5`/`spark` метод `_provider_configured`
возвращает `True` **просто по членству** в списке, не глядя на `f5_url`/`spark_url`.
Если запустить оркестратор напрямую (`uvicorn app.main:app`, без `run_api.sh`,
который выставляет URL-ы), то `/health` покажет `tts.enabled=true`, а первый же
`/voice` упадёт 502 (`_f5`/`_spark` бросят «URL не задан»). Health вводит в
заблуждение о готовности. Не критично (в проде запуск через `run_api.sh`), но
контракт «enabled» стоит привязать к наличию URL для f5/spark.

---

### 🟡 F12 — Полный текст ответа передаётся HTTP-заголовком

`app/main.py:252-268`: весь ответ (включая блок `[ТАБЛИЦА]`), percent-encoded,
уходит в заголовок `X-Answer`. Кириллица в UTF-8 percent-encoding раздувается
~в 6 раз (1 символ → `%D0%BF`), поэтому длинный ответ с таблицей легко даёт
заголовок в 8–12 КБ. Это едет через два звена (`:8000` → httpx во `video_ui` →
браузер) и упирается в лимиты размера заголовков h11/прокси. Функционально сейчас
проходит, но это хрупкий канал для растущих ответов.

**Рекомендация:** для длинных ответов рассмотреть отдачу текста телом
(multipart/JSON рядом с WAV) вместо заголовка, либо жёстко ограничивать длину
`X-Answer`.

---

### ⚪ F4 — Дрейф print-детекта Python ↔ JS

`app/service.py:167` (`_PRINT_RE_APP`) содержит `…подаёт|подач…`, а
`video_ui/static/answer_render.js:145` — `…подаёт|податч|подач…` (лишний `податч`,
похоже опечатка). В бою решает бэкенд (`X-Print`), JS — лишь demo-fallback, так что
это не баг, но два источника истины легко разъезжаются (уже разъехались). Стоит
подстраховать общим тест-вектором.

### ⚪ F5 — Пустой ответ LLM → лишний suggest-путь

`rag/ragsvc/rag_engine.py:124`: при пустом тексте от LLM возвращается фраза-отказ,
которую оркестратор ловит `looks_not_found` и запускает `suggest_question` — ещё
один медленный LLM-вызов на пустом/мусорном STT. По дизайну терпимо, но на «тишине
в микрофон» это лишняя задержка.

### ⚪ F9 — Бэкапы `rag_storage.bak.*` без ротации

На диске два `rag_storage.bak.*` (в git не попадают — корректно), но на сервере они
будут копиться при каждом переингесте. Добавить в runbook чистку старых бэкапов.

### ⚪ F10 — Тяжёлые бинарники в git

`talk.mp4` (16 МБ) + `idle.mp4` (5.5 МБ) + `.docx`-исходники (~4 МБ) закоммичены.
Для офлайн-переноса на АФМ это удобно (всё на месте), но история git пухнет —
кандидат в git-LFS, если репозиторий будет жить долго.

### ⚪ F11 — Мёртвая переменная в healthcheck

`scripts/healthcheck.sh:18` задаёт `F5=http://$HOST:8810`, но фактическую живость
F5 скрипт (правильно) читает из `api/health.tts.servers`. Переменная больше не
используется и при чтении шапки вводит в заблуждение (реальный F5 — `:8991`).

---

## 4. Безопасность — отдельно

- ✅ Секреты не в git; deny-list прав закрывает `.env`, `*secret*`, `*.pem`.
- ✅ Ошибки клиенту обезличены — внутренние адреса АФМ не утекают
  (`app/main.py:132`, `video_ui/server.py:140`). Аккуратно и сознательно.
- ✅ XSS: фронтенд строит DOM только `createElement`/`textContent`, `innerHTML`
  не используется даже для речи/ответа LLM (`answer_render.js`, `index.html:567`).
- ✅ QR только для `.kz` — сужение против «выдуманного домена» из LLM
  (`answer_render.js:186`).
- ✅ Prompt-injection: только логируется, не блокируется — **осознанный** выбор
  (temperature=0 + ответ строго по базе + жёсткий системный промпт с блоком
  «Защита от манипуляций»). Принятый риск, ок.
- ⚠️ См. F6 — открытый `:8000` + `/docs` в сети киоска.

---

## 5. Что сделано хорошо (не трогать)

- **Антигаллюцинация RAG**: `mode=naive` (только вектор, без графа — сознательно
  из-за медленного LLM), `temperature=0`, явный `COSINE_THRESHOLD`, языковые метки
  `[RU]/[KK]` на **каждом** чанке (включая «хвосты» длинных статей — см.
  `chunking._split_long_text`), цитата нормы словами, фирменная фраза-отказ с 1458.
- **Нормализация под TTS** (`app/clients/tts.py`) — самая сильная часть: раскрытие
  аббревиатур с падежами (ru + казахская морфология сингармонизма), числа→слова
  ru/kk с порядковыми и согласованием, телефоны группами с паузами, вырезание
  экранных таблиц из озвучки, подрезка «гуляющих» краёв F5 и фейд хвоста. Очень
  много выстраданных кейсов с обоснованиями в комментариях.
- **Мягкая деградация везде**: нет `num2words` → числа остаются цифрами, а не
  падение; несовместимые WAV при склейке — явная ошибка вместо тихого искажения;
  double-checked locking для загрузки Whisper; `{"data": null}` от STT → аккуратный
  502.
- **Языковой дедуп выдачи** без внешнего сервиса (`providers.lang_dedup_rerank`)
  через ContextVar — чисто и task-safe; внешний реранкер подключается тем же хуком.
- **Офлайн-first**: Swagger с локальных ассетов, tiktoken завендорен,
  `HF_HUB_OFFLINE`, QR-генератор в комплекте — под сеть без интернета.
- **Тесты и eval**: 51+16 зелёных, батарея проб `rag/eval/probe_battery.py`,
  `healthcheck.sh --full` — сквозная приёмка.

---

## 6. Приоритетный чек-лист перед пилотом

1. **[F1]** Починить health F5 (реальный `POST /tts` или `degraded`-статус) —
   иначе `healthcheck.sh` не даст «деплой принят», а мониторинг слепнет.
2. **[F2]** Решить казахский TTS: ElevenLabs (нужен интернет) **или** Spark +
   `SPEECH_RATE.kazakh = 1.4`. Сейчас медленный казахский на демо.
3. **[F8]** Зафиксировать SPOF ноды АФМ в `PILOT.md`, согласовать доступность с IT;
   продумать поведение при полном отказе RAG.
4. **[F6]** Свести `:8000` на loopback + firewall, закрыть `/docs` в проде.
5. **[F3]** Привязать `tts_enabled` к наличию `F5_URL/SPARK_URL`, чтобы `/health`
   не врал о готовности.
6. **[F12]** Оценить длину `X-Answer` на реальных ответах с таблицами; при риске —
   переносить текст в тело ответа.
7. **[F9/F10/F11]** Гигиена: ротация бэкапов RAG, LFS для видео (по желанию),
   убрать мёртвую `F5=` из healthcheck.

---

_Замечания F4–F5 и прочие Low — низкий приоритет, можно закрыть по ходу. Ни одно
из них не блокирует запуск._
