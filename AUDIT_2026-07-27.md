# Аудит Ai-dos — свежий независимый разбор

_Дата: 2026-07-27 · ветка `main` · метод: статический разбор всего первичного кода
(`app/`, `rag/ragsvc/`, `video_ui/`, `*_server/`, `run_api.sh`, `deploy/`, `scripts/`)
+ прогон локальных тестов. Живой стек АФМ (`192.168.165.2`, GPU-нода) по коду, не по
рантайму — с этой машины сеть АФМ недоступна, что честно помечено в находках.
Вендоренные репо и веса (`spark_tts_repo/`, `lightrag`, модели) — чёрный ящик._

Этот отчёт — **вход для to-spec → tickets → implement**. Каждая находка атомарна и
самодостаточна под тикет. Старые `AUDIT.md` / `AUDIT_TTS_F5.md` НЕ принимались на
веру: раздел «Сверка со старыми аудитами» в конце помечает F1–F12 как
живо/закрыто/устарело.

---

## 1. Итог (TL;DR)

Код **в зрелом инженерном состоянии**: чистые HTTP-границы между venv, серьёзная
антигаллюцинационная обвязка RAG (`temperature=0`, жёсткий промпт, языковой дедуп,
нормализация фразы-отказа), образцовая нормализация текста под TTS (падежи кодексов,
порядковые с согласованием, телефоны группами, ru+kk числа), XSS-безопасный офлайн-
фронтенд (только `createElement`/`textContent`), везде мягкая деградация. **Тесты
зелёные: 58 pytest + 16 node.** Голых `except:` нет, секреты в git не утекли
(`.env` и `logs/` в `.gitignore`, трекается только `.env.example`).

**Блокеров, ломающих сам ответ гражданину, не найдено.** Но есть находки, которые
на демо / 24-7 выстрелят — прежде всего **приёмка `healthcheck.sh` не пройдёт на
текущей пилотной топологии** и **расхождение systemd-юнитов с `run_api.sh`**.

| ID | Severity | Тема | Где |
|----|----------|------|-----|
| N1 | 🟠 High | `healthcheck.sh` всегда FAIL по F5 (у GPU-F5 :8991 нет `/health`) → приёмка не «зелёная» | `app/clients/tts.py`, `scripts/healthcheck.sh` |
| N2 | 🟠 High | Расхождение: systemd-юниты (local F5/Spark/Whisper) ≠ `run_api.sh` (F5 GPU + ElevenLabs) | `deploy/ai-dos-api.service`, `run_api.sh` |
| N3 | 🟡 Medium | `tts_enabled=true`, даже если `F5_URL`/`SPARK_URL` пусты — /health врёт «включён» | `app/config.py:157` |
| N4 | 🟡 Medium | ElevenLabs (дефолтный kk) вообще не пингуется в `/health` — падение облака заметит гражданин | `app/clients/tts.py:1253` |
| N5 | 🟡 Medium | Весь ответ (+таблица) едет percent-encoded в HTTP-заголовке `X-Answer` — латентный лимит | `app/main.py:312`, `video_ui/server.py:150` |
| N6 | 🟡 Medium | `:8000` слушает `0.0.0.0` без authn, `/docs` открыт | `app/main.py:97`, `run_api.sh:117` |
| N7 | 🟡 Medium | Темп kk-озвучки 1.0 жёстко зашит под ElevenLabs; при переключении на Spark речь медленная | `video_ui/static/index.html:322` |
| N8 | ⚪ Low | `whisper_kk_server` на deprecated `@app.on_event` + бинд `0.0.0.0` + `{e!r}` в 500 | `whisper_kk_server/server.py:78,107,112` |
| N9 | ⚪ Low | Дрейф print-регэкспа Python↔JS (`подач` vs `податч`) | `app/service.py:167`, `answer_render.js:145` |
| N10| ⚪ Low | Мёртвые переменные `F5=:8810`/`SPARK=:8809` в шапке healthcheck | `scripts/healthcheck.sh:18` |
| N11| ⚪ Low | Тяжёлые бинарники в git (`talk.mp4` 16 МБ, `idle.mp4` 5.5 МБ, `.docx`) | `video_ui/static/video/`, `rag/data/raw/` |
| N12| ⚪ Low | Семафор только на `/voice`; `/speak` может занять единственный TTS/GPU без лимита конкурентности | `app/main.py:176`, `app/config.py:139` |

---

## 2. Находки (ticket-ready)

### N1 · 🟠 High · `healthcheck.sh` всегда красит F5 в FAIL на текущей топологии
**Где:** `app/clients/tts.py:1253-1274` (`healthy()`), `scripts/healthcheck.sh:119-131`.
**Симптом:** `tts.healthy()` выводит `/health` из `F5_URL` (`.../tts` → `.../health`)
и пингует его. Пилотный дефолт русского TTS — multipart-сервер F5 на GPU-ноде АФМ
(`http://192.168.165.2:8991/tts`, см. `run_api.sh:87`), у которого, в отличие от
локального `f5_server`, эндпоинта `/health` нет. Пинг → 404/ошибка →
`reachable:false`. `scripts/healthcheck.sh` читает `tts.servers.f5.reachable` из
`/health` оркестратора и на `false` печатает `✘ FAIL`, а значит `exit 1` и «ГОТОВО:
деплой принят» НЕ печатается НИКОГДА на штатной топологии.
**Почему важно:** приёмочный скрипт — это ворота деплоя и вход watchdog/дашбордов.
Постоянный ложный «красный» по F5 (а) не даёт приёмке пройти, (б) маскирует
НАСТОЯЩЕЕ падение F5 — оператор не отличит норму от аварии. Прямой удар по 24/7.
**Направление фикса:** различать «TTS отвечает синтезом» и «есть `/health`». Варианты:
health-проба синтезом крошечного текста; ИЛИ добавить `/health` на GPU-F5; ИЛИ
конфиг-флаг «у этого F5 нет health — проверяй синтезом». Не полагаться на `/health`
там, где его контрактно нет.

### N2 · 🟠 High · systemd-юниты описывают ДРУГУЮ топологию, чем `run_api.sh`
**Где:** `deploy/ai-dos-api.service:11-18` против `run_api.sh:76-106`.
**Симптом:** юнит пинит `TTS_PROVIDER=f5 / F5_URL=http://localhost:8810/tts`
(локальный `f5_server`, JSON-контракт, без `F5_REF_AUDIO`), `TTS_KK_PROVIDER=spark /
SPARK_URL=localhost:8809`, `STT_KK_USE_WHISPER=true`, `WHISPER_DEVICE=cuda`. А текущий
пилотный дефолт (`run_api.sh`, `CLAUDE.md`): русский F5 — на GPU-ноде АФМ :8991
(multipart), казахский — **ElevenLabs** (облако), STT казахского — сервер АФМ (не
локальный Whisper).
**Почему важно:** systemd — это заявленный путь 24/7. Кто развернёт через юниты,
получит топологию, для которой должны быть подняты локальные `f5_server` + `spark` +
`whisper_kk` (torch/веса на самой ноде), а ElevenLabs-ключ проигнорируется. Тихая
рассинхронизация «как в docs» ↔ «как в юнитах» → долгий разбор на месте.
**Направление фикса:** свести к ОДНОМУ источнику истины. Либо юниты берут окружение
из общего `.env` (`EnvironmentFile=`), либо явно завести два набора юнитов
(local-TTS vs GPU+eleven) и в `deploy/README.md` указать, какой когда.

### N3 · 🟡 Medium · `tts_enabled` истинно даже при пустых URL провайдера
**Где:** `app/config.py:157-171` (`_provider_configured` / `tts_enabled`).
**Симптом:** для `f5`/`spark` конфиг считается валидным по ОДНОМУ факту `provider in
_KNOWN_TTS_PROVIDERS`, без проверки, что `f5_url`/`spark_url` заданы. `/health`
рапортует `tts.enabled=true`, а на реальном `/voice` синтез падает
`RuntimeError("F5_URL не задан")` → 502 гражданину.
**Почему важно:** пропущенный URL в `.env` не ловится ни на старте, ни в health —
только живым отказом у экрана. `eleven`/`openai` уже проверяются по-настоящему;
`f5`/`spark` — нет.
**Направление фикса:** в `_provider_configured` для `f5`/`spark` требовать непустой
соответствующий `*_url` (или явный «локальный без URL» режим). Тогда health/старт
поймают дыру раньше гражданина.

### N4 · 🟡 Medium · ElevenLabs (дефолтный казахский TTS) не пингуется в `/health`
**Где:** `app/clients/tts.py:1253-1274` (`healthy()` собирает только `f5`/`spark`).
**Симптом:** при пилотном дефолте `TTS_KK_PROVIDER=eleven` в `tts.servers`
нет вообще никакого сигнала о доступности облака/ключа. Первым признаком пропажи
интернета или протухшего ключа станет проваленный казахский ответ у гражданина.
**Почему важно:** kk-путь по умолчанию завязан на внешний сервис — самое хрупкое
звено (сеть АФМ офлайн, доступ к `api.elevenlabs.io` — по доброй воле IT), и именно
у него нет мониторинга.
**Направление фикса:** добавить лёгкую проверку eleven в `healthy()` (например, `GET`
`/v1/user` с ключом или дешёвый TTS-пинг с кэшем результата на N секунд, чтобы не
жечь символы), выводить `tts.servers.eleven.reachable`.

### N5 · 🟡 Medium · Текст ответа целиком едет в HTTP-заголовке `X-Answer`
**Где:** `app/main.py:312-328` (кладёт `quote(answer)` в заголовок),
`video_ui/server.py:150-156` (проксирует заголовки как есть), декод —
`video_ui/static/index.html:684`.
**Симптом:** тело `/voice` занято WAV, поэтому текст ответа (проза + блок
`[ТАБЛИЦА]`) percent-encode'ится в заголовок. Кириллица в `quote()` раздувается в
3–6 байт на символ; необычно длинный ответ (LLM проигнорировал «2–4 предложения»)
или крупная таблица приближают заголовок к лимитам h11/прокси/браузера. При превышении
средний хоп (`httpx` в `video_ui`) или браузер отбросят заголовок → киоск получит
звук БЕЗ текста на экране (караоке/таблица/QR не построятся), либо ответ оборвётся.
**Почему важно:** латентно и незаметно на «хороших» ответах, но деградация тихая —
именно на длинном/табличном ответе, где экран нужнее всего.
**Направление фикса:** унести текст из заголовка в тело. Напр. multipart-ответ
(WAV + JSON) или отдать метаданные отдельным JSON-эндпоинтом с тем же request-id, а
WAV — по ссылке. Как минимум — ограничить длину `X-Answer` и логировать усечение.

### N6 · 🟡 Medium · `:8000` слушает `0.0.0.0` без аутентификации, `/docs` открыт
**Где:** `app/main.py:97-106` (`/docs`), `run_api.sh:117` и
`deploy/ai-dos-api.service:20-22` (`--host 0.0.0.0`).
**Симптом:** оркестратор доступен всей сети киоска без authn; Swagger `/docs`
открыт. Любой в LAN может слать `/voice`/`/speak` (жечь GPU/TTS), читать схему API.
**Почему важно:** киоск-сеть редко «доверенная»; дорогие пути без authn — вектор DoS
и разведки. Юнит это признаёт комментарием, но по умолчанию всё равно `0.0.0.0`.
**Направление фикса:** за TLS-reverse-proxy сузить оркестратор до `127.0.0.1`; либо
basic-auth/сетевой ACL на `:8000`; `/docs` выключить в проде (`docs_url=None` за
флагом окружения). Решение операционное — оставляю на спеку.

### N7 · 🟡 Medium · Темп kk-озвучки жёстко 1.0 — верен только для ElevenLabs
**Где:** `video_ui/static/index.html:322` (`SPEECH_RATE = { kazakh: 1.0, russian: 1.0 }`).
**Симптом:** `playbackRate` казахского фиксирован 1.0. Это корректно для дефолтного
kk=ElevenLabs. Но при переключении на Spark (`TTS_KK_PROVIDER=spark` / `USE_LOCAL_SPARK=1`
— офлайн-режим без интернета) Spark говорит медленно (~8 c на тестовой фразе), и
фронт не знает о смене движка — гражданин слышит вялую речь. Значение зашито в
статике, о провайдере фронт не осведомлён.
**Почему важно:** офлайн-фолбэк (Spark) — это ровно тот сценарий, когда у АФМ нет
интернета, т.е. вероятный боевой режим; и именно там UX-регресс по темпу.
**Направление фикса:** прокидывать активный kk-провайдер на фронт (заголовок/поле в
ответе или `/health`) и выбирать `SPEECH_RATE` по нему (eleven→1.0, spark→~1.4).
Комментарий в коде уже описывает нужные числа.

### N8 · ⚪ Low · `whisper_kk_server`: deprecated `on_event` + бинд `0.0.0.0` + утечка в 500
**Где:** `whisper_kk_server/server.py:78` (`@app.on_event("startup")`), `:112`
(`host="0.0.0.0"` хардкодом, без `HOST`-env, в отличие от f5/spark), `:107`
(`detail=f"Whisper-kk error: {e!r}"`).
**Симптом:** `on_event` удаляют в новых FastAPI (нужен `lifespan`); порт открыт на
все интерфейсы без возможности сузить; сырой `{e!r}` в ответе может нести пути/детали.
**Почему важно:** отложенная поломка при апгрейде FastAPI; мелкая утечка (сервис
внутренний, на GPU-ноде) — потому Low.
**Направление фикса:** перевести на `lifespan`; вынести `HOST` в env (дефолт
`0.0.0.0` для GPU-ноды ок, но настраиваемо); обезличить 500 наружу, детали — в лог.

### N9 · ⚪ Low · Дрейф print-регэкспа Python↔JS
**Где:** `app/service.py:167-169` (`...подать|подаёт|подач|...`) против
`video_ui/static/answer_render.js:145` (`...подать|подаёт|податч|подач|...`).
**Симптом:** JS-версия содержит лишний альтернант `податч`, которого нет в Python.
**Почему важно:** мало: в бою решает `X-Print` бэкенда (авторитет), JS-детект —
фолбэк только для demo без бэкенда. Расхождение косметическое, но это «мина
рассинхрона» на будущее.
**Направление фикса:** свести к единому списку триггеров (или тест-контракт,
сверяющий обе стороны).

### N10 · ⚪ Low · Мёртвые переменные в шапке healthcheck
**Где:** `scripts/healthcheck.sh:18-19` (`F5=...:8810`, `SPARK=...:8809`).
**Симптом:** после перехода на чтение `tts.servers.*` из `/health` оркестратора эти
переменные больше не используются.
**Направление фикса:** удалить, чтобы не сбивать с толку (создают ложное ощущение,
что healthcheck ходит на :8810/:8809 напрямую).

### N11 · ⚪ Low · Тяжёлые бинарники закоммичены в git
**Где:** `video_ui/static/video/talk.mp4` (16 МБ), `idle.mp4` (5.5 МБ),
`rag/data/raw/*.docx` (КоАП/УПК, сотни КБ).
**Симптом:** раздувают репозиторий и каждый клон; `.docx` — исходники, из которых
генерятся `data/*.md`.
**Направление фикса:** git-lfs для роликов ИЛИ отдавать их отдельным артефактом;
`.docx` — в LFS или вынести из репо (в `.md` они уже сконвертированы).

### N12 · ⚪ Low · Семафор конкурентности только на `/voice`
**Где:** `app/main.py:29` (`_voice_sem`) применён только в `/voice`; `/speak`
(`:176`), `/chat`, `/transcribe` — без него.
**Симптом:** `/speak` ограничен лишь `max_speak_chars`, но не числом одновременных —
пачка прямых `/speak` может занять единственный TTS/GPU-ресурс.
**Почему важно:** киоск ходит через `/voice` (защищён), поэтому Low; но `/speak`
публичен на `0.0.0.0` (см. N6).
**Направление фикса:** распространить семафор на `/speak` (общий лимит TTS-конкурентности).

---

## 3. Что проверено и НЕ вызвало нареканий

- **Тесты:** `58 passed` (pytest, `.venv`), `16 pass` (node `answer_render.test.js`).
- **XSS-фронтенд:** ответ LLM и вопрос гражданина рендерятся ТОЛЬКО через
  `createElement`/`textContent`; QR-`img.src` — из наших словарей/URL; `openZoom`
  клонирует свой же DOM. Дыр не нашёл.
- **Мягкая деградация:** LLM-коррекция/подсказка при сбое отдают сырой текст и
  ЛОГируют (`app/service.py`); пустой/битый STT (`{"data": null}`) даёт аккуратный
  502, не 500 (`app/clients/stt.py:124`); пустой ответ LLM → каноническая фраза-отказ
  (`rag_engine.py:172`); Spark на вырожденной генерации ретраит и отдаёт 422
  (`spark_server/server.py:94`); склейка WAV падает явно на несовместимых форматах
  (`tts.py:851`).
- **Антигаллюцинация RAG:** `temperature=0`, `enable_llm_cache=0` (не отдаёт устаревшее
  после переингеста), нормализация англ. заглушки LightRAG и чужеязычного отказа,
  срез «фантомной» отсылки к несуществующей таблице, языковой дедуп выдачи.
- **Приватность:** `logs/` и `.env` в `.gitignore`; `log_questions` full|hash|off;
  суточная ротация JSONL = ретеншен ПДн; наружу httpx-ошибки обезличены (не палят
  внутренние адреса АФМ).
- **Конкурентность серверов:** `f5`/`spark` сериализуют инференс `threading.Lock`
  (модели не потокобезопасны); double-checked locking при ленивой загрузке Whisper.
- Голых `except:` нет; секреты в git не утекли.

---

## 4. Сверка со старыми аудитами (`AUDIT.md`, `AUDIT_TTS_F5.md`)

| Старый ID | Статус сейчас | Комментарий |
|-----------|---------------|-------------|
| F1 (health F5 `reachable:false`) | 🔴 ЖИВО → **N1** | Усугубилось: healthcheck теперь на этом ставит `exit 1`. |
| F2 (темп kk под eleven, стек — Spark) | 🟡 ЧАСТИЧНО → **N7** | Дефолт сменился на eleven, поэтому 1.0 теперь ВЕРЕН по умолчанию; риск только при офлайн-Spark. Downgrade High→Medium. |
| F3 (`tts_enabled` при пустых URL) | 🔴 ЖИВО → **N3** | Без изменений. |
| F4 (дрейф print-регэкспов) | 🟡 ЖИВО → **N9** | По-прежнему расходятся; влияние Low (backend авторитетен). |
| F5 (пустой ответ → лишний suggest) | 🟢 В ОСНОВНОМ ЗАКРЫТО | `rag_engine.py:172` теперь отдаёт каноничный отказ; пустой ответ обрабатывается штатно (доп. suggest на пути отказа — уже by design). |
| F6 (`0.0.0.0`, `/docs` открыт) | 🔴 ЖИВО → **N6** | Без изменений. |
| F8 (SPOF: LLM+эмбеддинг+STT+F5 на одной ноде) | 🔴 ЖИВО (архитектура) | Фолбэка нет; kk-TTS ушёл в облако (eleven) — на этот сервис SPOF снят, но добавил зависимость от интернета (N4). |
| F9 (`rag_storage.bak.*` без ротации) | 🟡 ЖИВО (Low) | Gitignored; копятся на диске сервера. Не выносил в отдельный N — ops-гигиена. |
| F10 (видео/`.docx` в git) | 🟡 ЖИВО → **N11** | Без изменений. |
| F11 (мёртвая `F5=:8810`) | 🟡 ЖИВО → **N10** | Плюс мёртвая `SPARK=:8809`. |
| F12 (весь ответ в `X-Answer`) | 🟡 ЖИВО → **N5** | Без изменений; латентный лимит заголовка. |

**Новое, чего в старых аудитах не было:** N2 (расхождение systemd ↔ `run_api.sh` —
появилось после перехода на GPU-F5 + ElevenLabs), N4 (нет health-пинга eleven —
следствие того же перехода), N8 (deprecated `on_event` в `whisper_kk_server`), N12
(семафор только на `/voice`).

---

## 5. Рекомендованный порядок для tickets → implement

1. **N1, N2** (High, операционные ворота) — без них приёмка и 24/7-мониторинг врут.
2. **N3, N4** (Medium, наблюдаемость) — дёшево, закрывают слепые зоны health.
3. **N5, N6** (Medium, архитектура/безопасность) — требуют решения (формат ответа,
   TLS/ACL); занести в спеку как решения, не как механический фикс.
4. **N7** (Medium, UX офлайн-фолбэка).
5. **N8–N12** (Low) — гигиена/дренаж технического долга, пакетом.
