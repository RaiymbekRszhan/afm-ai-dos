# Аудит Ai-dos — 2026-07-31

_Ветка `main` (`1c41770`) · метод: статический разбор изменений от 31.07 плюс
сверка старых аудитов с текущим кодом · 271 pytest + 68 node + 46 rag зелёные._

Повод: за день в бой уехали три изменения (зонд `/diag`, выбор языка озвучки по
тексту ответа с правкой промпта RAG, перезагрузка страниц флота с сервера), а
пилот при этом уже стоит во всех 20 регионах. Проверялось прежде всего то, что
меняли сегодня, — свежий код на боевых точках опаснее старого.

---

## 0. Статус прежних аудитов

| файл | дата | статус |
|---|---|---|
| `AUDIT_2026-07-27.md` | 27.07 | **ЗАКРЫТ ПОЛНОСТЬЮ** — все N1–N12 исправлены веткой pilot-hardening (`365f6c5`) |
| `AUDIT.md` | 24.07 | устарел, помечен таковым уже в аудите от 27.07 |
| `AUDIT_TTS_F5.md` | 27.07 | устарел, там же |

Сверено по коду, а не по памяти: `X-Answer` больше нет — ответ едет JSON-телом
(N5); `/docs` за `settings.enable_docs` (N6); `tts_enabled` стал вычисляемым
свойством (N3); регэксп печати в Python и JS сошёлся на `подач` (N9); семафор
`_tts_sem` накрыл и `/speak` (N12).

**Вывод: открытых задач в старых аудитах нет.** Держим их как хронику — по ним
видно, почему код устроен так, а не иначе.

---

## 1. Находки

**ВСЕ ПЯТЬ ИСПРАВЛЕНЫ 31.07** — коммиты `2a102f4`, `bbb95d2`, `d92f33c`,
`6509d86`, `0cae4c4`. Итог: 278 pytest + 74 node + 46 rag.

| N | важность | суть | где | статус |
|---|---|---|---|---|
| A1 | 🟡 Medium | `with_print_offer` дописывает приглашение на языке КИОСКА, а не ответа → казахская фраза в русском ответе | `app/service.py`, `app/main.py` | ✅ `2a102f4` |
| A2 | 🟡 Medium | Отключённую рубильником точку невозможно перезагрузить с сервера; `armReload` крутит таймер вечно | `video_ui/static/index.html` | ✅ `bbb95d2` |
| A3 | ⚪ Low | В отпечаток версии страницы попадают `*.test.js` и админка — правка теста перезагрузит 20 экранов | `app/kiosks.py` | ✅ `d92f33c` |
| A4 | ⚪ Low | `/diag/stream` публичен, мимо проходной и лимита темпа; потолки щедрые (10,5 МБ за запрос) | `video_ui/server.py` | ✅ `6509d86` |
| A5 | ⚪ Low | `provider` в аналитике = догадка по переключателю, если синтез не состоялся | `app/main.py` | ✅ `0cae4c4` |

### A1 · 🟡 Medium · Приглашение распечатать бланк смешивает языки

`service.with_print_offer(answer, lang)` выбирает язык фразы по `lang`, то есть по
переключателю на киоске. Но язык ОТВЕТА задаёт RAG — он отвечает на языке вопроса
(правило 1 промпта). Гражданин на казахском киоске спросил по-русски → ответ
русский → в конец приклеивается казахское «Қаласаңыз, өтініш үлгісін…».

Это ровно тот дефект, что 31.07 чинили в промпте RAG (казахский ответ с русской
вставкой), только приходящий со стороны кода. Хуже того, он ломает и озвучку:
`tts.detect_lang` увидит преимущественно русский текст, отдаст всё F5, и русский
голос прочитает казахское предложение.

**Как чинить:** язык приглашения брать из текста ответа — `tts.detect_lang(answer, lang)`.

### A2 · 🟡 Medium · Отключённую точку нельзя перезагрузить с сервера

Рубильник (`applyBlocked`) ставит `btn.disabled = true`. `kioskIdle()` требует
`!btn.disabled`, поэтому на отключённой точке простой не наступает НИКОГДА:
`armReload` уходит в бесконечный ретрай раз в 3 секунды и не перезагружается.

Ирония в том, что это ровно та ситуация, когда обновить страницу нужнее всего:
точку выключили, чтобы починить. Плюс бесконечный ретрай без сдачи.

**Как чинить:** отключённая точка простаивает по определению — гражданин к ней не
обращается, кнопка нажата быть не может. Считать её idle.

### A3 · ⚪ Low · Тестовые и админские файлы влияют на версию страницы

`kiosks._ui_fingerprint()` берёт все `*.html/js/css` из `video_ui/static/`. Там
лежат пять `*.test.js`, `admin.html` и `admin_util.js` — браузер киоска не
загружает ни один из них. Правка теста или админки перезагрузит 20 публичных
экранов без причины.

**Как чинить:** считать отпечаток только по тому, что киоск реально загружает.

### A4 · ⚪ Low · `/diag/stream` без проходной и лимита темпа

Зонд живёт на video_ui (:80), а проходная (`_gate`) и `KIOSK_RATE_PER_MIN` — на
:8000. При `n=40&size=262144` один запрос отдаёт 10,5 МБ, а `gap_ms=0` делает это
мгновенно. Сеть АФМ внутренняя и наружу порт не торчит, поэтому важность низкая,
но потолки щедрее, чем нужно: зонду хватает 8 кусков по 16 КБ.

**Как чинить:** урезать потолки до разумного (n ≤ 20, size ≤ 64 КБ).

### A5 · ⚪ Low · Провайдер в аналитике может врать

`_new_record` пишет движок по языку переключателя, фактический подставляется
только после УСПЕШНОГО синтеза (`rec["provider"] = used_provider`). При ошибке TTS
или `TTS_ENABLED=false` в журнале останется движок, который не работал, — и отчёт
по провайдерам покажет несуществующую нагрузку.

**Как чинить:** при неудаче писать пусто, а не догадку.

---

## 2. Что проверено и нареканий не вызвало

- **определение языка озвучки** (`tts.detect_lang`) — покрыто тестами с обеих
  сторон и на смешанном тексте; экранная таблица из определения исключена, порог
  доли 1% защищает от одиночной чужой буквы;
- **версия страницы** кэшируется на 5 с — залп из 20 пингов стоит один обход
  каталога; `kiosks-reload.txt` в `.gitignore` (иначе выкатка кода перезагружала
  бы флот);
- **перезагрузка не рвёт ответ**: гейт по простою и защита от цикла (2 минуты)
  вынесены в `reload_guard.js` чистыми функциями и покрыты тестами;
- **зонд ходит POST**, как боевой `/voice/stream` — прокси обращается с методами
  по-разному, проверка по GET соврала бы;
- секреты (`.env`, `rag/.env`, `kiosks-keys.txt`) на месте и в `.gitignore`.

---

## 3. Исправлено (порядок: сначала видимое гражданину, потом эксплуатация, потом гигиена)

| коммит | что сделано |
|---|---|
| `2a102f4` | A1 — язык приглашения берётся из ТЕКСТА ответа (`tts.detect_lang`), а не из переключателя |
| `bbb95d2` | A2 — отключённая точка считается простаивающей; правило вынесено в `reload_guard.isIdle()` чистой функцией и покрыто тестами (инлайн в `index.html` оно было непроверяемо) |
| `d92f33c` | A3 — `*.test.js`, `admin.html`, `admin_util.js`, `diag.html` исключены из отпечатка; ключ отпечатка — путь, а не голое имя (одноимённые файлы в подпапках сливались) |
| `6509d86` | A4 — потолки зонда: n 40→20, gap 5000→2000, size 256→64 КБ |
| `0cae4c4` | A5 — `provider` пуст до синтеза; страница пустого не боится (`speechRate(null)` = 1.0) |

Два решения по ходу стоит запомнить:

- **список исключений в A3 — ИСКЛЮЧАЮЩИЙ, а не разрешающий.** Новый модуль
  страницы попадёт в отпечаток сам; забудь мы дописать его в разрешающий список —
  точки остались бы со старым кодом, то есть ровно с той бедой, ради которой
  перезагрузка флота и делалась;
- **A2 переехал в модуль не ради красоты.** Дефект был именно того класса, что
  ловится юнит-тестом за секунду, но жил в инлайн-скрипте, где тестов нет.
