# Аудит русского TTS (F5-TTS_RUSSIAN)

_Дата: 2026-07-27 · ветка `feature/logging` · статический разбор кода + сверка с живыми артефактами (референсы, тесты)._

Область: **только русский синтез речи (провайдер `f5`)** — от `synthesize()` в оркестраторе
до сервера F5 и обратно. Казахский (Spark), ElevenLabs и STT затронуты лишь там, где
пересекаются с F5. Тесты `tests/test_helpers.py` — 34 passed на момент аудита.

## Что читалось
- [app/clients/tts.py](app/clients/tts.py) — нормализация, нарезка, склейка, `_f5`, подрезка тишины
- [f5_server/server.py](f5_server/server.py), [f5_server/run.sh](f5_server/run.sh), [f5_server/setup.sh](f5_server/setup.sh), [f5_server/transcribe_ref.py](f5_server/transcribe_ref.py)
- [app/config.py](app/config.py), [app/main.py](app/main.py) (`/voice`, `/speak`), [run_api.sh](run_api.sh)
- `refs/ref_ru*.wav`, `refs/ref_ru.txt` (длительности/транскрипт сверены)

---

## 1. Итог (TL;DR)

Слой **инженерно зрелый**: нормализация текста под озвучку (падежи кодексов, порядковые
с согласованием, телефоны группами, транслит) — образцовая; два контракта F5 (JSON локальный
/ multipart GPU-АФМ) разделены чисто; подрезка краёв и гашение хвоста решают реально
слышанные на демо артефакты; всё деградирует мягко, а не падает.

Но есть **один воспроизводимый баг в фолбэк-пути**, **один незакрытый вопрос качества,
от которого зависит весь смысл выбора F5**, и **дыра в тестах ровно на самом хрупком
(ручную подобранном) участке звука**. Ниже — по убыванию важности.

| # | Категория | Серьёзность |
|---|-----------|-------------|
| B1 | `USE_LOCAL_F5=1` шлёт multipart на JSON-сервер → 422 | ✅ **исправлено 2026-07-27** |
| R1 | RUAccent на `:8991` — ударения ставятся | ✅ **подтверждено 2026-07-27** |
| B2 | «Смена голоса» не обновляет реально используемый `_padded`-референс | 🟠 баг процедуры |
| R2 | У F5 нет ретраев/деградации на кусок (в отличие от Spark) | 🟠 надёжность |
| M1 | Нулевое тестовое покрытие подрезки тишины / гашения / пауз | 🟠 пробел |
| I1–I4 | Эффективность и мелкие несогласованности | 🟢 улучшения |

---

## 2. 🔴 Баги / ошибочные места

### B1. Фолбэк `USE_LOCAL_F5=1` сломан: multipart летит на JSON-сервер
> **СТАТУС: ✅ исправлено 2026-07-27.** В [run_api.sh](run_api.sh) блок TTS разведён по
> режиму: при `USE_LOCAL_F5=1` оркестратор получает пустой `F5_REF_AUDIO` (→ JSON-контракт)
> и дефолтный `F5_URL=http://localhost:8810/tts`; иначе — прежний multipart на `:8991`.
> Проверено матрицей резолва (оба режима + внешние переопределения). Описание бага — ниже.

[run_api.sh:80](run_api.sh#L80) экспортировал `F5_REF_AUDIO=refs/ref_ru_f5_padded.wav`
**безусловно** — ветка `USE_LOCAL_F5` ([run_api.sh:52-53](run_api.sh#L52-L53)) только
поднимает локальный сервер, но `F5_REF_AUDIO` не сбрасывает.

В `_f5` выбор контракта — по факту заданности референса:
[tts.py:1152](app/clients/tts.py#L1152) `if settings.f5_ref_audio:` → **multipart**
`{ref_audio, ref_text, gen_text}`. А локальный [f5_server/server.py:118-119](f5_server/server.py#L118-L119)
принимает **только JSON** `{text, language}` (pydantic `TTSRequest`, поле `text` обязательно).

Итог: документированная команда
`USE_LOCAL_F5=1 F5_URL=http://localhost:8810/tts bash run_api.sh` ([run_api.sh:51](run_api.sh#L51))
переопределяет `F5_URL`, но **не** `F5_REF_AUDIO` → оркестратор шлёт multipart на локальный
сервер → FastAPI отвечает `422 Unprocessable Entity` (нет поля `text`) → весь `/voice`
падает в 502. Фолбэк, ради которого флаг и существует, не работает.

**Фикс:** в ветке `USE_LOCAL_F5=1` очищать референс, чтобы включился JSON-контракт:
```bash
if [ "$USE_LOCAL_F5" = "1" ]; then
  export F5_REF_AUDIO=""            # локальный f5_server = JSON-контракт, референс на сервере
  export F5_URL="${F5_URL:-http://localhost:8810/tts}"
  bash f5_server/run.sh &
fi
```
(и убрать `F5_URL` из обязательных в подсказке — он станет дефолтным). Заодно стоит
переставить дефолт `F5_URL`/`F5_REF_AUDIO` так, чтобы локальная ветка перекрывала
GPU-дефолт, а не наоборот.

### B2. «Смена голоса» не трогает реально используемый референс
Дефолтный референс в бою — `refs/ref_ru_f5_padded.wav` ([run_api.sh:80](run_api.sh#L80)):
тот же голос + 250/300 мс тишины по краям, чинит «резкий старт» (F5 клонировал манеру
начинать с нулевого сэмпла и съедал атаку первого слова).

А задокументированная процедура смены голоса ([f5_server/README.md:44](f5_server/README.md#L44))
запускает [transcribe_ref.py](f5_server/transcribe_ref.py), который пишет
`refs/ref_ru_f5.wav` + `refs/ref_ru.txt` ([transcribe_ref.py:25-26](f5_server/transcribe_ref.py#L25-L26)) —
но **не** `_padded`-файл. Скрипта, добавляющего тишину по краям, в репозитории нет вовсе.

Итог: сменив голос по инструкции, получаешь новый `ref_ru_f5.wav`, но бой продолжает
клонировать **старый** `ref_ru_f5_padded.wav` — новый голос молча игнорируется, а если
вручную подменить на непаддингованный, возвращается «резкий старт». Плюс локальный
[f5_server/run.sh:12](f5_server/run.sh#L12) использует непаддингованный `ref_ru_f5.wav` —
то есть два пути (локальный и GPU) звучат по-разному «из коробки».

**Фикс:** дописать паддинг тишины в `transcribe_ref.py` (выдавать сразу `_padded`),
либо вынести шаг в отдельный воспроизводимый скрипт `pad_ref.py` и сослаться на него в README;
привести `f5_server/run.sh` к тому же `_padded`-референсу.

---

## 3. 🔴 Риск качества, который надо закрыть до пилота

### R1. Ударения (RUAccent) на боевом сервере `:8991`
> **СТАТУС: ✅ подтверждено 2026-07-27** (пользователь): RUAccent на `:8991` стоит, ударения
> ставятся на боевом пути. Риск снят. Разбор ниже — для контекста, почему это было важно.

Весь смысл выбора F5 вместо XTTS — **правильные ударения через RUAccent** (XTTS их врал;
это записано и в [f5_server/README.md](f5_server/README.md), и в памяти проекта).

- Локальный [f5_server/server.py:151](f5_server/server.py#L151) действительно прогоняет
  `_accent.process_all(gen_text)` перед синтезом. ✅
- Боевой путь — **multipart на GPU-сервер АФМ `:8991`** ([run_api.sh:75](run_api.sh#L75)),
  и его кода в репозитории **нет**. Клиент `_f5` отправляет `gen_text` **без разметки
  ударений** ([tts.py:1165-1170](app/clients/tts.py#L1165-L1170)) — RUAccent живёт только
  в `.venv-f5` и в лёгком оркестраторе отсутствует по дизайну.

Если сервис на `:8991` **не** запускает RUAccent сам, то в бою русский синтезируется
**без ударений** — то есть ровно та проблема, ради ухода от которой брали F5.

Опаснее всего, что это **не ловится существующими проверками**: все валидации «слово в слово
по Whisper» (память 2026-07-17) сверяют *состав слов*, а Whisper не различает ударение.
Значит, корректность ударений на боевом пути не проверялась ни разу.

**Действие:** явно подтвердить у коллеги/по коду `:8991`, что там стоит RUAccent
(та же `omograph_model_size`, тот же токен-патч `token_type_ids`); на слух прогнать
слова-омографы («зАмок/замОк», «стОит/стоИт», «Уже/ужЕ») через боевой `:8991`.
Если RUAccent там нет — либо поднять его на `:8991`, либо отдавать текст уже
с «+»-разметкой (тогда RUAccent придётся тащить в оркестратор — против замысла «лёгкого киоска»).

---

## 4. 🟠 Надёжность

### R2. У F5 нет ретраев и деградации на уровне куска
Spark обёрнут защитой: `SPARK_RETRIES` + вырожденная генерация отдаёт `422`, а не рушит
ответ (память, `spark_server`). У F5 такой обвязки нет: `_f5` делает
`resp.raise_for_status()` ([tts.py:1176](app/clients/tts.py#L1176)) без ретрая, а
`synthesize` синтезирует куски строго последовательно
([tts.py:909](app/clients/tts.py#L909) `[await _synthesize_one(part, …) for part in parts]`).
Любой единичный сбой (таймаут/5xx/битый WAV) на **одном** из ~10 кусков длинного ответа
поднимает исключение → [main.py:304-307](app/main.py#L304-L307) → `502`, гражданин не
слышит **ничего**, хотя 9 кусков синтезировались успешно.

**Действие:** добавить 1–2 ретрая с бэкоффом на кусок (сеть/`5xx`) по образцу Spark;
опционально — не валить весь ответ из-за одного куска (пропустить/повторить), раз склейка
это позволяет.

### R3. `_infer_lock` на локальном сервере — единая точка блокировки
[f5_server/server.py:127](f5_server/server.py#L127): весь инференс сериализован глобальным
`threading.Lock` (правильно — F5/RUAccent мутируют глобальный стейт). Но если один
`infer` подвиснет, лок держится, и **все** последующие запросы висят до таймаута клиента
(httpx `timeout=180`, [tts.py:1151](app/clients/tts.py#L1151)); клиент отвалится, а поток
сервера останется занят. Касается только локального фолбэк-сервера, но при `USE_LOCAL_F5`
на CPU (медленный синтез) это реально. Смягчение: таймаут/watchdog на инференс на стороне
сервера, либо явный лимит очереди.

---

## 5. 🟠 Пробел в тестах

### M1. Самый хрупкий звук — без единого теста
Ручную подобранные по слуху куски **не покрыты вовсе**:
- `_trim_edge_silence` + удаление «хвоста-сироты» с порогами `_BLOB_JOIN_MS/_BLOB_GAP_MS/
  _BLOB_MAX_MS/_BLOB_MIN_SPEECH_MS/_ONSET_THRESH` ([tts.py:1002-1066](app/clients/tts.py#L1002-L1066));
- `_fade_out_tail` ([tts.py:1069-1104](app/clients/tts.py#L1069-L1104));
- `_gap_ms_after` (полная/половинная пауза по знаку, [tts.py:809-821](app/clients/tts.py#L809-L821)).

Именно здесь возвращался «страшный звук» на демо, а границы порогов «тонкие»
(естественные паузы до ~190 мс против отрыва артефакта 180–230 мс — из комментариев).
Любой рефакторинг может молча их сдвинуть: регрессию поймают только ушами на следующем демо.

**Действие:** юнит-тесты на синтетических WAV (собрать int16-буфер с известными
сегментами речи и паузами): проверить, что (а) края ровняются до `_EDGE_KEEP_MS`,
(б) короткий оторванный хвост срезается, (в) естественная пауза внутри речи — нет,
(г) `_fade_out_tail` монотонно гасит последние N мс и не трогает остальное,
(д) `_gap_ms_after` даёт `base` после `.!?` и `base//2` после запятой. Всё это чистые
функции над WAV-байтами — тестируются без сети и без модели.

---

## 6. 🟢 Улучшения (эффективность и порядок)

- **I1. Подрезка/гашение — питоновские посэмпловые циклы.** `_trim_edge_silence`
  ([tts.py:1025](app/clients/tts.py#L1025)) и `_fade_out_tail`
  ([tts.py:1093](app/clients/tts.py#L1093)) идут по каждому int16-сэмплу на чистом Python;
  для куска ~11–13 c (24 кГц ≈ 300k сэмплов) это заметные десятки–сотни мс на кусок, на
  каждый ответ. `array` — stdlib, numpy в venv оркестратора нет по замыслу, поэтому хотя бы:
  считать «громкость» срезами/`max` по окну, а не поэлементным генератором в `any(...)`.
- **I2. Новый `httpx.AsyncClient` на каждый кусок.** `_f5` открывает клиент на каждый вызов
  ([tts.py:1151](app/clients/tts.py#L1151)); ответ из 10 кусков = 10 установок соединения к
  одному хосту. Общий модульный клиент (или пул) уберёт накладные и латентность рукопожатий.
  То же в `_spark`/`_eleven`.
- **I3. Локальный путь звучит иначе, чем боевой.** [f5_server/run.sh](f5_server/run.sh):
  `NFE=16`, `cpu`, непаддингованный референс, и клиент в JSON-режиме **не** добавляет
  точечный паддинг gen_text ([tts.py:1165](app/clients/tts.py#L1165) — только в multipart-ветке).
  Паддинг — воркэраунд под «впритык» длительность именно сервера `:8991`; библиотечный F5
  считает длительность сам, так что для локального это, скорее всего, ок — но расхождение
  стоит зафиксировать в доке, чтобы фолбэк не удивлял на слух.
- **I4. `/speak` без семафора.** `/voice` защищён `_voice_sem`
  ([main.py:29](app/main.py#L29), [main.py:232](app/main.py#L232)); `/speak`
  ([main.py:176-194](app/main.py#L176-L194)) — нет (это осознанно отмечено в
  [config.py:127-132](app/config.py#L127-L132), защита — только лимит длины `max_speak_chars`).
  Порт слушает `0.0.0.0` — пачка `/speak` может занять единственный GPU-ресурс. Для отладочного
  эндпоинта терпимо, но на киоске стоит либо закрыть `/speak`, либо накрыть тем же семафором.

---

## 7. Что сделано хорошо (для баланса)

- **Нормализация под озвучку** — сильнейшая часть: раскрытие кодексов с падежом
  ([tts.py:99-118](app/clients/tts.py#L99-L118)), порядковые с согласованием по роду/падежу,
  телефоны группами с паузами-запятыми, транслит латиницы фолбэком после словаря брендов.
- **Два контракта F5** разведены аккуратно и покрыты тестами
  ([test_helpers.py:305-359](tests/test_helpers.py#L305-L359)).
- **Проверки на старте сервера** (ckpt/vocab/референс — падаем сразу с понятным сообщением,
  а не молчаливой загрузкой английской модели в офлайне, [f5_server/server.py:60-66](f5_server/server.py#L60-L66)).
- **Референс сверен:** `ref_ru_f5.wav` = 11.0 c, транскрипт `ref_ru.txt` = 197 симв./22 слова
  (~18 симв/с) — правдоподобно совпадает; `transcribe_ref.py` транскрибирует именно
  обрезанный клип, так что ref_text ↔ ref_audio согласованы (в пределах паддинга).
- **Мягкая деградация:** пустой/табличный текст → отсылка к экрану, а не 5xx
  ([tts.py:874-879](app/clients/tts.py#L874-L879)); подрезка/гашение при не-WAV возвращают
  вход как есть, не роняя синтез.

---

## 8. Порядок действий (рекомендация)

- [x] **R1** — RUAccent на `:8991` подтверждён (2026-07-27). ✅
- [x] **B1** — `USE_LOCAL_F5` починен: контракт разведён по режиму (2026-07-27). ✅
- [ ] **R2** — ретрай/деградация на кусок у F5 (сейчас один сбой = тишина в ответ).
- [ ] **B2** — привести смену голоса к `_padded`-референсу (скрипт + README).
- [ ] **M1** — юнит-тесты на подрезку тишины/гашение/паузы (заморозить ручную настройку).
- [ ] **I1–I4** — по возможности.
