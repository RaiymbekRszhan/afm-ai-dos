# Пилот Ai-dos — инструкция (бэкенд на Linux + видео-аватар)

Пилотный запуск «цифрового офицера» АФМ. Этот файл — **сквозной runbook**: как поднять
бэкенд на Linux и завести фронтенд (киоск с видео-аватаром, вариант 2), со всеми граблями.
Установка бэкенда по шагам — в [DEPLOY.md](DEPLOY.md); здесь ссылки на него, а не дубли.

---

## 0. Топология пилота (вариант 2 «видео-аватар» — выбран руководителем)

```
 ┌───────────────────────────────┐
 │  Киоск (браузер, DS-65T)      │
 │  http://<LINUX-IP>:8100/      │
 │  ├ видео: idle / «говорит»    │   записанные ролики
 │  └ микрофон → /voice ─проксӣ──┼──┐
 └───────────────────────────────┘  │
                                     ▼
        ┌──────────────────────────── Linux-сервер (GPU) ───────────────────────────┐
        │  :8100 video_ui  ·  :8000 оркестратор  ·  :8077 RAG  ·  :8809 Spark(kk)     │
        └───────────────────────────────────────────────────────────────────────────┘
                     │ LLM 192.168.165.2:8901 · эмбеддинги :8806 · STT ru :8804 · ru-TTS :8991
```

**Одна машина** — Linux-сервер: весь бэкенд + страница с роликами. Ни Windows, ни
GPU-рендера, нагрузка почти нулевая. Гражданин жмёт «Задать вопрос» → микрофон → тот же
пайплайн STT→RAG→TTS → на странице играет WAV ответа, поверх — немой ролик «аватар
говорит», по окончании звука обратно на idle. Детали — [video_ui/README.md](video_ui/README.md).

```bash
bash run_api.sh        # ВЕСЬ стек одной командой: Spark + RAG + бэкенд (:8000) + киоск (:8100)
```

---

# ЧАСТЬ A. БЭКЕНД (Linux)

## A0. Предполётная проверка (это решает, взлетит ли пилот)
Риск пилота — не в коде (пайплайн доказан вживую), а в инфраструктуре. **До установки**
ответь на 4 вопроса прямо на сервере АФМ — от них зависит весь план:

1. **Интернет для установки?** `curl -I https://pypi.org` и `https://huggingface.co`.
   - Есть → обычный путь (DEPLOY.md).
   - Нет (HTTP 000) → **офлайн-путь** (см. A6): все 4 venv и модели ставятся бандлом под
     Linux, собранным заранее на машине с интернетом. Это главный пожиратель времени —
     закладывай его первым.
2. **GPU на этом Linux-боксе?** `nvidia-smi`. Whisper-kk (казахский STT) и Spark (kk TTS)
   хотят CUDA. Если GPU нет — kk-ветка будет тормозить/не поднимется; ru-ветка (STT/TTS с
   АФМ-сервера) выживет. Нужен GPU-бокс, которым мы управляем, **не** только общий
   инференс-сервер `192.168.165.2`.
3. **Сеть в обе стороны?** С этого бокса: `curl -m5 http://192.168.165.2:8901/v1/models`
   (LLM), `:8806` (эмбеддинги для RAG), `:8804/...` (ru STT), `:8991/...` (ru TTS). И
   наоборот — `:8000` и `:8100` бокса должны быть видны киоску (firewall!). Самый
   непредсказуемый пункт — вопрос к сетевикам АФМ.

> **Стратегия:** сначала подними **только бэкенд** и прогони `scripts/healthcheck.sh --full`
> — это доказывает ~80% и не зависит от аватара. Аватар/киоск подключай вторым этапом.

## A1. Установка
Полностью по [DEPLOY.md](DEPLOY.md) (шаги 0–8): 4 сервиса, каждый в своём venv, `uv`/`pip`,
RAG-индекс через `ingest`, `.env`, запуск `HF_HUB_OFFLINE=1 bash run_api.sh`, приёмка
`bash scripts/healthcheck.sh --full`. Здесь — только пилотные акценты.

**Грабли, которые кусают именно на Linux/офлайн (все уже разобраны в DEPLOY.md):**
- **venv НЕ копируем с Mac** — пересоздаём на месте (`.venv`, `rag/.venv`, `.venv-spark`).
- **`rag/` venv нельзя сливать с основным** — `lightrag`/`torch` конфликтуют.
- **tiktoken-словарь** — папка `rag/vendor/tiktoken/` обязана приехать, иначе RAG офлайн не стартует.
- **Казахский STT — на сервере АФМ** (`STT_KK_USE_WHISPER=false`): локальный Whisper-kk
  (~1.6 ГБ, torch) на этой машине НЕ нужен. Вернуть его — HF-кэш модели + `STT_KK_USE_WHISPER=true`.
- **GPU** (для офлайн-фолбэка TTS): в `spark_server/run.sh` — `SPARK_DEVICE="cuda"`.

## A2. Ключевые настройки `.env` (корень проекта)
Полный список — DEPLOY.md шаг 6. Обязательные для пилота:
```
LLM_BASE_URL=http://192.168.165.2:8901/v1
STT_URL=http://192.168.165.2:8804/transcribe     # STT АФМ — и русский, И казахский
STT_KK_USE_WHISPER=false                          # kk STT на сервер АФМ (без Whisper/torch)
RAG_URL=http://localhost:8077/ask
# TTS ОСНОВНОЙ — ElevenLabs (⚠️ нужен интернет). Старт: USE_ELEVEN=1 bash run_api.sh
ELEVENLABS_API_KEY=...
ELEVENLABS_VOICE_ID=...
# TTS ОФЛАЙН-ФОЛБЭК (дефолт run_api.sh без USE_ELEVEN):
TTS_PROVIDER=f5
F5_URL=http://192.168.165.2:8991/tts             # ru TTS с GPU-сервера АФМ (~1.5 c)
F5_REF_AUDIO=refs/ref_ru_f5_padded.wav  # с тишиной по краям — мягкие старты
F5_REF_TEXT=@refs/ref_ru.txt
TTS_KK_PROVIDER=spark
SPARK_URL=http://localhost:8809/tts
```

## A4. Порт 8000 наружу
Оркестратор слушает `0.0.0.0:8000`. Проверь firewall Linux, чтобы киоск и render-ПК
достучались: `curl http://<LINUX-IP>:8000/health`. Приёмка по сети: `HOST=<LINUX-IP>
bash scripts/healthcheck.sh`.

## A4a. Настройка голоса (грабли, оплаченные кровью 2026-07-16)

| Хочу | Крутить | НЕ крутить |
|---|---|---|
| Казахский говорит медленно | `SPEECH_RATE.kazakh` в `video_ui/static/index.html` (сейчас 1.0 — eleven в темпе; для Spark вернуть 1.4) | ❌ `SPARK_SPEED` |
| Русский говорит не так | референс `F5_REF_AUDIO` + `F5_REF_TEXT` (F5 клонирует голос вместе с темпом) | — |
| Куски налезают друг на друга | `TTS_GAP_MS` (сейчас 300 мс после точки, половина — после запятой) | — |

- **`SPARK_SPEED` не трогать.** `very_high` ломает синтез всегда (0 токенов, звука нет),
  `high` даёт всего ~10% темпа, но случайно роняет ответ в 422. Держим `moderate`
  (6/6 прогонов чисто), а скорость берём плеером — там она бесплатна и безотказна.
  Подробности и цифры — [spark_server/README.md](spark_server/README.md).
- **Spark нестабилен сам по себе** — изредка выдаёт вырожденную генерацию. Ретраи
  подняты до 6 (`SPARK_RETRIES=5` в `spark_server/run.sh`); если все попытки пусты,
  сервер отдаёт 422, и весь ответ падает. Не снижать.
- **Синтез растёт квадратично** от длины куска (100 симв. → 18 c, 330 симв. → 117 c на
  CPU). Поэтому текст режется: `TTS_MAX_CHARS=180` (предел F5!) и `TTS_KK_MAX_CHARS=260`
  (у Spark свой предел 3000 — запас нужен, чтобы разрез попал на запятую, а не в
  середину фразы, иначе Spark комкает хвост огрызка).
- **На CPU это минуты.** Один кусок — 33–54 c; длинный ответ из нескольких кусков идёт
  пару минут. Это мак без GPU, а не поломка: на сервере с `SPARK_DEVICE=cuda` — в разы
  быстрее. Первый казахский вопрос всегда медленный (Whisper грузит модель ~1.6 ГБ) —
  **прогоняй один вхолостую перед показом**.

## A5. 24/7
Боевой режим — systemd + сторож `/health`: [deploy/README.md](deploy/README.md)
(автозапуск, рестарт при падении/зависании). `run_api.sh` — для отладки. Долгий прогон
24/7 на Linux ещё не обкатан — на пилоте держи глаз на `/health` и логах первые сутки.

## A6. Если на сервере НЕТ интернета (офлайн-путь)
Сеть АФМ без интернета — тогда pip/HF/CDN недоступны, и обычная установка не пройдёт.
Что нельзя скачать на месте и надо привезти бандлом, собранным **под Linux** на машине с
интернетом (не с Mac — колёса платформозависимы):
- **Python-колёса всех 4 venv** — `uv pip download -r requirements.txt` (и `rag/`, f5, spark)
  в папки, затем на сервере `uv pip install --no-index --find-links <папка> -r ...`.
  `torch`/`torchaudio` — самые тяжёлые; f5/spark пинуют конкретные версии (см. их `requirements.txt`).
- **Модель Whisper-kk** (~1.6 ГБ) — привезти HF-кэш, запускать с `HF_HUB_OFFLINE=1`.
- **Модели f5/spark** — их `setup.sh` тянут модель; в офлайне подложить заранее.
- **tiktoken-словарь** — `rag/vendor/tiktoken/` + `TIKTOKEN_CACHE_DIR` (уже в `run_api.sh`/юните).

Сборка бандла — отдельная работа под конкретный дистрибутив/CUDA сервера. Если A0.1 показал
«нет интернета» — **это первый и самый долгий шаг**; спланируй его до всего остального.
DEPLOY.md шаг 0 про это же.

---

# ЧАСТЬ B. ФРОНТЕНД (вариант 2 «видео-аватар», :8100)

Страницу отдаёт `video_ui/` — детали в [video_ui/README.md](video_ui/README.md). Здесь
только пилотные акценты.

## B1. Как это собрано
- `bash video_ui/run.sh` поднимает страницу на `:8100`; вопросы она **проксирует** на
  `:8000/voice`, свой STT/RAG/TTS не поднимает — работает на том же пайплайне, что и Часть A.
- Ролики лежат в `video_ui/static/video/` (`idle.mp4` — молчит, `talk.mp4` — говорит,
  зациклен на длину звука). **Имена строго в нижнем регистре** — на Linux регистр важен,
  иначе 404 и чёрный экран. Проверка: `curl :8100/health` → `videos: {idle.mp4:true, talk.mp4:true}`.
- Темп речи — `SPEECH_RATE` в `video_ui/static/index.html` (kk 1.4 / ru 1.0), см.
  [A4a](#a4a-настройка-голоса-грабли-оплаченные-кровью-2026-07-16).

## B2. Открыть киоск
```
http://<LINUX-IP>:8100/?kiosk=1&ar=15/16
```
- `?kiosk=1` — режим тонкого клиента (крупные кнопки, бейдж языка, видео сверху).
- `?ar=15/16` — пропорция под аспект роликов (1080×1152). В киоске это дефолт, но при
  другом размере роликов передавай явно.
- Автозвук: по первому клику «Задать вопрос» страница снимает autoplay-блок (тихая
  заглушка внутри жеста), отдельной плашки нет.

## B3. ⚠️ Микрофон по http блокируется (secure-context) — обязательный гвоздь
Браузер даёт доступ к микрофону (`getUserMedia`) **только в secure-context**: HTTPS или
`localhost`. По `http://<LINUX-IP>:8100/` из киоска микрофон **молча не запишет** (кнопка
«Задать вопрос» ничего не отправит). Три варианта, по возрастанию правильности:
1. **Флаг браузера (быстро, для пилота)** — в Chrome/Edge открыть
   `chrome://flags/#unsafely-treat-insecure-origin-as-secure`, вписать
   `http://<LINUX-IP>:8100`, Enabled, перезапустить браузер. Делается на **каждом** киоске.
2. **HTTPS с самоподписанным сертификатом** — reverse-proxy (nginx/caddy) перед `:8100`,
   в киоск-браузер импортировать корневой сертификат.
3. **Правильно** — валидный TLS (если у АФМ есть внутренний CA/домен).

> Само аудио ОТВЕТА (проигрывание на странице) от secure-context НЕ зависит — оно упирается
> только в жест (autoplay). Secure-context нужен именно для ЗАПИСИ микрофона.



---

## C. Быстрая проверка пилота (чек-лист)
1. `curl http://<LINUX-IP>:8000/health` → `status:ok`, `rag.reachable:true`, `tts.enabled:true`.
2. `bash scripts/healthcheck.sh --full` (на сервере) → все ✔, сквозной STT→RAG→TTS.
3. `curl http://<LINUX-IP>:8100/health` → `backend_reachable:true`, `videos:{idle.mp4:true, talk.mp4:true}`.
4. Открыть `:8100/?kiosk=1&ar=15/16` на киоске → крутится idle-ролик.
5. Задать вопрос голосом (на пилоте — казахский, если ru-сервисы АФМ не подняты):
   играет звук ответа + ролик «говорит», по окончании — idle.
6. Микрофон на киоске работает по http (secure-context — §B3): нажми «Задать вопрос»,
   в статусе не должно быть «нет доступа к микрофону».

## D. Если что-то не так
| Симптом | Причина / лечение |
|---------|-------------------|
| Кнопка «Задать вопрос» не пишет / «нет доступа к микрофону» | Микрофон по http заблокирован (не secure-context) → флаг браузера или HTTPS (§B3) |
| Чёрный экран, «Нет роликов аватара» | Ролики не в нижнем регистре или не на месте → `curl :8100/health` (`videos`) |
| Видео есть, звука ответа нет | Не было жеста → нажми «Задать вопрос» ещё раз |
| Казахский голос тянет / рвётся | Темп — `SPEECH_RATE` в `video_ui/static/index.html`; паузы/нарезка — `TTS_GAP_MS`/`TTS_KK_MAX_CHARS` (см. §A4a) |
| `/voice` висит ~3 мин и падает | LLM/TTS АФМ не подняты (RAG ждёт LLM, TTS ждёт F5) — `scripts/healthcheck.sh`, доступность 192.168.165.2 |
| RAG не стартует офлайн | tiktoken-словарь `rag/vendor/tiktoken/` (DEPLOY.md шаг 3) |

Контакты сервисов АФМ (LLM/эмбеддинги/STT/ru-TTS) — в DEPLOY.md.

