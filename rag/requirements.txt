lightrag-hku==1.4.9.10
openai>=1.30
numpy
tiktoken
fastapi
uvicorn[standard]
python-dotenv

# pytest (юнит-тесты tests/) — НЕ здесь, чтобы не тащить тестовый инструмент в
# прод-venv. Для разработки: pip install pytest (или pyproject.toml extra "dev").
