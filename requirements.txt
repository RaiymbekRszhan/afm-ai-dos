fastapi==0.115.6
uvicorn[standard]==0.34.0
openai==1.59.6
httpx==0.28.1
numpy==2.2.1
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20
num2words==0.5.14        # числа -> слова для русского TTS (F5 цифры не читает)

# Локальный казахский STT (Whisper-kk, STT_KK_USE_WHISPER=true) — ТЯЖЁЛЫЙ стек
# (torch/transformers/librosa) вынесен в requirements-whisper-kk.txt. По умолчанию
# НЕ нужен: казахский STT идёт на сервер АФМ (:8804) или на whisper_kk_server,
# оркестратор без него лёгкий и ставится БЕЗ torch/CUDA (импорты в stt.py ленивые,
# прогрев Whisper гейтится флагом). См. requirements-whisper-kk.txt.

